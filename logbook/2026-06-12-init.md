# 2026-06-12 — Repository initialised (TC-STN-INIT-01)

**What changed.** Created the repository for *The Statistical Turn in Trapped-Ion Noise
Diagnostics*; declared the FAIR research profile (**full**); placed the Sail and the
six/eight profile artefacts; copied the Model B assets and the §14A checker.

**Pins (verified, not recalled).**
- Blueprint **CD 1.8.2**, `cd-rules`@`a61ad52ff46bcb64874dba1de84d49d681e06629`
  (committer `uwarring82`, 2026-06-12T09:52:52Z) — verified via the GitHub commits API.
- Six design assets copied from the pin (repo **root**, not `assets/`); `sha256sum`
  matches `assets/SOURCE.md` by direct computation.
- `tools/fair_check.sh` v1.8.2 copied from the pin; SHA-256
  `d6090b77f83d2bb063fcffad78c5ec2118694c32af5ded252dab97b83f1f7ba0` matches
  `tools/SOURCE.md`.

**Decisions.**
- **D-1** name → `noise-diagnostics`.
- **D-2** profile → full, minimal floor stood up first.
- **D-3** Sail content = CC BY-NC-SA 4.0; Model B copies + any future tooling = MIT.
- **D-4** primary entry surface → `README.md` (declared `entry-surface: README.md`).
- **D-5** Model B source anchor → **EC correction.** The card proposed cutting a new tag
  `cd-1.8.2-freeze`. Verification found an **annotated tag `cd-1.8.2` already exists and
  points to the pin `a61ad52`** (message: "CD 1.8.2 — §14A FAIR … frozen as Coastline").
  That is the named drift-detection anchor §0.10 wants, so **no second tag was cut**
  (§0.12, single source of truth); provenance pins to `cd-1.8.2`. The §2/D-5 premise
  "no tag exists / no releases published" was stale at read-time.

**Bibliography (FR8) — verify-before-assert.** The Sail explicitly cites Brownnutt
*et al.* 2015 and imports Holevo / Vacchini structure theorems and the quantum linear
Boltzmann equation (Tier Map §2). Three identifiers were verified and entered:
- Brownnutt *et al.* 2015 — `10.1103/RevModPhys.87.1419` (arXiv:1409.6572).
- Holevo 1996, *Covariant quantum Markovian evolutions* — `10.1063/1.531481`.
- Vacchini & Hornberger 2009, *Quantum linear Boltzmann equation* —
  `10.1016/j.physrep.2009.06.001` (arXiv:0904.3911).
Each DOI resolves at `doi.org`; titles/authors confirmed via Crossref; arXiv ids via the
arXiv API. A remembered Vacchini DOI (`…/j.physrep.2009.04.001`) was **wrong** (it is a
polymer-dynamics paper) and was discarded — exactly the fabrication surface §8.D.3 warns
of. **Pedernales *et al.* 2015** (App. G) is **not cited in the Sail** and was **not
entered**. The reviewer leg (record-matches-work, note-faithful) remains open.

**Sail placement.** `essays/the-statistical-turn-in-trapped-ion-noise-diagnostics.md`
copied verbatim from `uwarring82/ions-in-freiburg`@`41059f9` (2026-02-20), keeping its
Endorsement Marker v2.0; a CC BY-NC-SA 4.0 SPDX header was added.

**Supersedes.** Nothing — initial entry.

**Namespace.** Published under **`uwarring82/noise-diagnostics`** (not the
`threehouse-plus-ec` org): the human account owns the publish surface and can create the
repo directly. CITATION `url`/`repository-code` point there so the FR2 URL resolves.
Provenance pins to `cd-rules`@`a61ad52` are unchanged.

**Open / next.** Close the FR8 reviewer leg; confirm the CITATION ORCID; record the
first green `fair_check.sh` run below once the repo is live.

**First green run.** `bash tools/fair_check.sh` exit 0 on 2026-06-12 — FR2 repo URL live (HTTP 200), CFF validates (1.2.0), 3/3 DOIs resolve; CI half of §14A satisfied. Reviewer half (FR2 ORCID, FR4/FR6 plain-language, FR8 faithfulness) remains open.

---

## 2026-06-12 — Anchoring references added (FR8 expansion)

Expanded the bibliography from 3 to **14 verified entries**, grouped by the Sail's
imports: ion-trap heating (Brownnutt, Turchette, Deslauriers), open-system generators
(Davies, Gorini–Kossakowski–Sudarshan, Lindblad, Holevo, Breuer & Petruccione),
collisional decoherence / QLBE (Hornberger, Vacchini & Hornberger), and Lévy / heavy-tail
statistics (Sato, Metzler & Klafter, Clauset–Shalizi–Newman, Hill).

Verify-before-assert caught two wrong DOIs from memory: `10.1007/BF01608389` is **Davies
1974**, not Lindblad (real Lindblad = `10.1007/BF01608499`); a guessed Sato/Cambridge DOI
resolved to an unrelated Morimoto book, so Sato is entered by **OpenLibrary-verified ISBN
9780521553025** instead. Metzler & Klafter's DOI contains parentheses that the checker's
extractor truncates, so it is linked in **percent-encoded** form (resolves 302).

OA split: 🟢 6 open access (Brownnutt, Turchette, Deslauriers, Hornberger, Vacchini,
Clauset via arXiv; Hill free at Project Euclid) · 🔒 7 paywalled for the maintainer to
fetch (Davies, GKS, Lindblad, Holevo, Breuer & Petruccione, Metzler & Klafter, Sato).
`bash tools/fair_check.sh` → exit 0 (14/14 entries, 13/13 DOIs resolve).

---

## 2026-06-12 — Local source copies assembled (untracked)

Collected full-text PDFs for the 14-entry bibliography into `docs/sources/pdfs/`, kept
**untracked** via `.gitignore` (§0.1: the derived record is the committed substitute;
the FR8 denylist fails on tracked PDFs outside `figures/`). Manifest:
`docs/sources/README.md`. Files are named by BibTeX key.

- Maintainer-provided (🔒 paywalled), renamed to keys: davies1974, gks1976, lindblad1976,
  holevo1996, breuer2007 (the book), metzler2000.
- Fetched (🟢 OA), verified each is the correct complete PDF: brownnutt2015,
  turchette2000, deslauriers2006, hornberger2006, vacchini2009, clauset2009 (arXiv);
  hill1975 (Project Euclid free PDF via the Internet Archive — a scanned image, confirmed
  by rendering page 1).
- **`sato1999` not held:** no legitimate open-access full text of the Cambridge monograph
  exists; needs library/ILL/purchase. Free substitute for the Lévy–Khintchine background
  recorded in the manifest (Applebaum, AMS Notices 2004).

13 of 14 sources held locally; checker remains exit 0 (all PDFs untracked).

---

## 2026-06-12 — GitHub Pages site rendering the Sail

Stood up a Jekyll site (deploy-from-branch) so the essay renders here, ahead of retiring
the GitBook copy. Work: converted the essay's GitBook export to clean GitHub-Flavoured
Markdown — `{% hint %}` → HTML callout; un-escaped GitBook LaTeX (`\$$`, `\_`, `\[`,
`\}`); normalised inline math to `$$…$$` (kramdown emits `\(…\)`/`\[…\]`, rendered by
MathJax 2.7). Repaired three table/list cells where absolute-value/conditional bars
(`|Δp|`, `P(n|τ)`) collided with GFM pipes — one had lost content in the GitBook export
and was reconstructed from §5.3's own formulas (flagged for reviewer fidelity). Added a
self-contained layout (`_layouts/default.html`), `_config.yml`, and an `index.md` landing;
gave GLOSSARY/BIBLIOGRAPHY/OUTREACH front matter so they render too. Repo homepage
repointed from GitBook to https://uwarring82.github.io/noise-diagnostics/ . Both CI
workflows green (FAIR §14A check + pages-build-deployment); FAIR checker exit 0.

---

## 2026-06-12 — Source notes (FR8 derived record)

Built `docs/source-notes.md`: own-words notes on all 14 sources, formulas in the Sail's notation, each independently verified against its PDF (4 drafts corrected). It is a §0.1 derived record (PDFs stay untracked) and feeds the FR8 reviewer leg (record-matches-work / note-faithful). Added a one-line **Key mapping** to each BIBLIOGRAPHY entry; the notes link uses an absolute GitHub URL (the `docs/` tree is excluded from the Pages site, so a repo-relative link 404s there).

---

## 2026-06-12 — Pages site adopts the Corporate Design (CD 1.8.2)

The Jekyll layout previously used ad-hoc CSS, inconsistent with the T(h)reehouse +EC
Corporate Design. Reworked `_layouts/default.html` to link the Model-B `assets/tokens.css`
and apply the CD system per §5 (palette) / §6 (typography) / §7 (spacing) / §8 (lockup) /
§12A (accessibility): Crimson Pro body on `--parchment` with a faint grid, IBM Plex Mono
labels/nav in `--sea`, `--signal` reserved for `+EC`/corrections (K1), open-edge tables
with horizontal separators only, sea-edged callouts, 1.5rem section rules, the emblem+
wordmark lockup, a wordmark colophon, skip-link/focus-rings/44px targets. `_config.yml`
now serves `assets/` on Pages (only `SOURCE.md`/`LICENCE` excluded); emblem-16 is the
favicon (F12). MathJax retained; pages-build + FAIR checks green; checker exit 0.

---

## 2026-06-12 — Source notes rendered on the site; table-spacing fix

Two Pages refinements. (1) **Tables:** the layout forced `display:block` on tables, which
broke column auto-sizing (non-wrapping math columns over-wide, text columns squeezed).
Now tables keep `display:table` with `table-layout:auto`, wrapped by a JS-inserted
`.table-scroll` region for horizontal overflow — columns size to content. (2) **Source
notes:** moved `docs/source-notes.md` → `source-notes.md`, rendered it as a CD-styled site
page (`/source-notes.html`), added it to the nav, and linked it from `BIBLIOGRAPHY.md`.
Math: normalised 877 inline `$…$` → `$$…$$` (kramdown only renders `$$`), then escaped
raw `<`,`>`,`|` inside math to `\lt`,`\gt`,`\vert` — those characters broke kramdown's
math-span recognition (it escaped `<`→`&lt;` and read `|` as a table pipe), cascading 62
leaked spans; now 0 leaks, 982 rendered. pages-build + FAIR checks green; checker exit 0.
